class Solution {
public:
    int climbStairs(int n) {
        int count = 0;
        cout<<factorial(5);
        // for (int i = 0; i < n/2; ++i)
        // {
            // int upper = 0;
            // count += n-2*iCi;
        // }
    }
};